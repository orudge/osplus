KNOWN PROBLEMS
==============

- Sometimes certain converters (especially the Win32 ones) don't always work, or produce an empty
  temporary file.

- Error message is garbled when trying to open an "invalid" file (e.g., passing "/?" on the
  command line.)